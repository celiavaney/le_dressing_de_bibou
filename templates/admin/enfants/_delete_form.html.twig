<form method="post" action="{{ path('app_admin_enfants_delete', {'id': enfant.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet enfant&nbsp;?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfant.id) }}">
	<button class="btn p-0">
		<i class="fa-solid fa-trash text-danger"></i>
	</button>
</form>
