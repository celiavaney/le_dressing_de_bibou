{% extends 'base.html.twig' %}

{% block title %}Edit Enfants{% endblock %}

{% block body %}
    <h1>Edit Enfants</h1>

    {{ include('admin/enfants/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_enfants_index') }}">back to list</a>

    {{ include('admin/enfants/_delete_form.html.twig') }}
{% endblock %}
