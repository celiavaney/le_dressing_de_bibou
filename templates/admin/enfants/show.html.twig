{% extends 'base.html.twig' %}

{% block title %}Enfants{% endblock %}

{% block body %}
    <h1>Enfants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enfant.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enfant.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_enfants_index') }}">back to list</a>

    <a href="{{ path('app_admin_enfants_edit', {'id': enfant.id}) }}">edit</a>

    {{ include('admin/enfants/_delete_form.html.twig') }}
{% endblock %}
