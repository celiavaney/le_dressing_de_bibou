{% extends 'base.html.twig' %}

{% block title %}Le dressing de Bibou - Page client admin
{% endblock %}

{% block body %}
	<div class="nom-appli">Le dressing de Bibou</div>
	<h1 class=" text-center p-3">Espace administrateur&nbsp;:
		<br>
		{{ user.email }}
	</h1>

	<div class="col-11 mx-auto">
		<table class="table table-striped table-bordered">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ user.id }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ user.email }}</td>
				</tr>
				<tr>
					<th>Roles</th>
					<td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
				</tr>
			</tbody>
		</table>
	</div>

	<div class="d-flex flex-column gap-3 align-items-center my-5">
		<a class='button-violet-clair btn' href="{{ path('app_admin_user_index') }}">Retour à la liste clients</a>
		<div class="d-flex gap-3 justify-content-center align-items-center">
			<a href="{{ path('app_admin_user_edit', {'id': user.id}) }}">
				<i class="fa-solid fa-pen-to-square"></i>
			</a>
			{{ include('admin/user/_delete_form.html.twig') }}
		</div>
	</div>

{% endblock %}
