{% extends 'base.html.twig' %}

{% block title %}Tailles{% endblock %}

{% block body %}
    <h1>Tailles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taille.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ taille.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_tailles_index') }}">back to list</a>

    <a href="{{ path('app_admin_tailles_edit', {'id': taille.id}) }}">edit</a>

    {{ include('admin/tailles/_delete_form.html.twig') }}
{% endblock %}
