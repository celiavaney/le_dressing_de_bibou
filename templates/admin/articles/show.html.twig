{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    <h1>Articles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ article.nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ article.photo }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ article.sexe }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>PrixAchete</th>
                <td>{{ article.prixAchete }}</td>
            </tr>
            <tr>
                <th>PrixVente</th>
                <td>{{ article.prixVente }}</td>
            </tr>
            <tr>
                <th>OffertPar</th>
                <td>{{ article.offertPar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_articles_index') }}">back to list</a>

    <a href="{{ path('app_admin_articles_edit', {'id': article.id}) }}">edit</a>

    {{ include('admin/articles/_delete_form.html.twig') }}
{% endblock %}
